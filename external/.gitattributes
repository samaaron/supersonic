# Force LF on the patch files. Without this, Windows git with
# core.autocrlf=true rewrites them to CRLF on checkout, so the patches no
# longer match the LF-line-ended Link source and `git apply` fails.
*.patch text eol=lf

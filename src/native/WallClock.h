/*
 * WallClock.h — current wall-clock time as NTP seconds.
 * The definition lives in clock_math.h (shared with the cross-process
 * sample-clock readers); this header remains for its native includers.
 */
#pragma once

#include "clock_math.h"
